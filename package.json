{
  "name": "baths",
  "private": true,
  "scripts": {
    "start": "npm run camunda:boot && npm run workers",
    "project": "npm run project:db && npm run camunda:boot && npm run project:apps",
    "project:db": "npm run db:full --prefix backend",
    "project:apps": "concurrently -k -n backend,frontend,workers -c blue,green,magenta \"npm start --prefix backend\" \"npm start --prefix frontend\" \"npm run workers\"",
    "camunda:up": "docker compose up -d",
    "camunda:logs": "docker compose logs -f camunda",
    "camunda:down": "docker compose down",
    "camunda:clean": "docker compose down -v",
    "camunda:reset": "docker compose down -v && docker compose up -d --build",
    "camunda:restart": "docker compose down && docker compose up -d",
    "camunda:deploy": "node ./camunda/deploy-bpmn.js",
    "camunda:boot": "npm run camunda:up && node ./camunda/deploy-bpmn.js",
    "camunda:wipe-processes": "node ./camunda/wipe-processes.js",
    "workers": "cross-env TZ=UTC node camunda/workers/index.js",
    "workers:dev": "cross-env TZ=UTC nodemon camunda/workers/index.js",
    "prisma:generate": "npx prisma generate --schema=backend/prisma/schema.prisma",
    "camunda:test-single": "cross-env TZ=UTC node ./camunda/test-single.mjs"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0",
    "camunda-external-task-client-js": "^3.1.0",
    "date-fns": "^4.1.0",
    "dotenv": "^17.4.0",
    "nodemailer": "^8.0.4"
  },
  "devDependencies": {
    "concurrently": "^9.1.0",
    "cross-env": "^10.1.0",
    "nodemon": "^3.1.0",
    "prisma": "^6.19.0"
  }
}
